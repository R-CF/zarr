# Register a Zarr domain for this session

Register a Zarr domain for this session

## Usage

``` r
zarr_register_domain(domain)
```

## Arguments

- domain:

  An instance of a class descending from `zarr_domain`.

## Value

Nothing.
