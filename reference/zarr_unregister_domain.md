# Unregister a Zarr domain from this session

Unregister a Zarr domain from this session

## Usage

``` r
zarr_unregister_domain(domain)
```

## Arguments

- domain:

  The name of the `zarr_domain` to unregister.

## Value

Nothing.
