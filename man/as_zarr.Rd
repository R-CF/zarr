% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{as_zarr}
\alias{as_zarr}
\title{Convert an R object into a Zarr object.}
\usage{
as_zarr(x, store = NULL)
}
\arguments{
\item{x}{The R object to convert into a Zarr object. Must be a vector, matrix
or array of a numeric or logical type.}

\item{store}{Optional. A location on a local file system where to persist the
data. The location must be writable by the calling code. As per the Zarr
specification, it is recommended to use a name that ends in ".zarr". If the
\code{store} argument is not given, a Zarr object is created in memory.}
}
\value{
The Zarr object, or an error if the Zarr object could not be created.
}
\description{
This function creates a Zarr object from an R vector, matrix or array. The
Zarr object will be a single Zarr array, with default settings derived from
the R object (data type, shape) and this package (fill_value, chunk_grid,
codecs).
}
