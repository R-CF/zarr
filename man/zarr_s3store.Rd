% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store_s3.R
\docType{class}
\name{zarr_s3store}
\alias{zarr_s3store}
\title{Zarr Store for AWS S3 (and S3-compatible) access}
\description{
This class implements a Zarr S3 store, read/write capable for
authenticated users. It uses the \code{paws.storage} package for all S3
operations, including credential resolution, so this class never handles
secret keys directly unless the caller explicitly passes them through.

Like \link{zarr_httpstore}, this class will look for consolidated metadata
(\code{.zmetadata}) or a \code{zarr.json} / \code{.zarray} / \code{.zgroup} document at the
configured prefix to identify the store version and root node. Unlike the
HTTP store, this class does not \emph{need} consolidated metadata to enumerate
nodes: S3's \code{ListObjectsV2} API is used directly for \code{list_dir()} and
\code{list_prefix()}, so the store reflects the live state of the bucket.

Credentials are resolved by \code{paws.storage} using its standard provider
chain (environment variables, shared credentials file / profile, IAM
role or instance/task metadata, SSO). Pass \code{profile} to select a named
profile, or explicit \code{access_key}/\code{secret_key}/\code{session_token} for
credentials threaded through from elsewhere. If none of those are given,
the store defaults to anonymous (unsigned) requests, since
\code{paws.storage} does not fall back to anonymous access on its own and
the common case for a freshly-opened store is a public bucket; pass
\code{anonymous = FALSE} explicitly to force normal credential resolution
with none of the above supplied (e.g. to pick up IAM role / instance
metadata credentials with no explicit profile or key).

NOTE: this class performs no sanity checks on any of the arguments
passed to the methods, for performance reasons, consistent with
\link{zarr_httpstore}. It should be accessed through group and array objects.
}
\section{Super class}{
\code{\link[zarr:zarr_store]{zarr_store}} -> \code{zarr_s3store}
}
\section{Active bindings}{
  \if{html}{\out{<div class="r6-active-bindings">}}
  \describe{
    \item{\code{friendlyClassName}}{(read-only) Name of the class for printing.}

    \item{\code{root}}{(read-only) The bucket and prefix of the store, as \verb{s3://}.}

    \item{\code{uri}}{(read-only) The URI of the store location, identical to \code{root}.}

    \item{\code{separator}}{(read-only) The default chunk separator of the store,
usually a slash '/'.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-zarr_s3store-initialize}{\code{zarr_s3store$new()}}
    \item \href{#method-zarr_s3store-exists}{\code{zarr_s3store$exists()}}
    \item \href{#method-zarr_s3store-clear}{\code{zarr_s3store$clear()}}
    \item \href{#method-zarr_s3store-erase}{\code{zarr_s3store$erase()}}
    \item \href{#method-zarr_s3store-erase_prefix}{\code{zarr_s3store$erase_prefix()}}
    \item \href{#method-zarr_s3store-list_dir}{\code{zarr_s3store$list_dir()}}
    \item \href{#method-zarr_s3store-list_prefix}{\code{zarr_s3store$list_prefix()}}
    \item \href{#method-zarr_s3store-list}{\code{zarr_s3store$list()}}
    \item \href{#method-zarr_s3store-getsize}{\code{zarr_s3store$getsize()}}
    \item \href{#method-zarr_s3store-getsize_prefix}{\code{zarr_s3store$getsize_prefix()}}
    \item \href{#method-zarr_s3store-is_empty}{\code{zarr_s3store$is_empty()}}
    \item \href{#method-zarr_s3store-set}{\code{zarr_s3store$set()}}
    \item \href{#method-zarr_s3store-set_if_not_exists}{\code{zarr_s3store$set_if_not_exists()}}
    \item \href{#method-zarr_s3store-get}{\code{zarr_s3store$get()}}
    \item \href{#method-zarr_s3store-get_metadata}{\code{zarr_s3store$get_metadata()}}
    \item \href{#method-zarr_s3store-set_metadata}{\code{zarr_s3store$set_metadata()}}
    \item \href{#method-zarr_s3store-is_group}{\code{zarr_s3store$is_group()}}
    \item \href{#method-zarr_s3store-create_group}{\code{zarr_s3store$create_group()}}
    \item \href{#method-zarr_s3store-create_array}{\code{zarr_s3store$create_array()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-initialize}{}}}
\subsection{\code{zarr_s3store$new()}}{
  Create an instance of this class.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$new(
  bucket,
  prefix = "",
  region = NULL,
  profile = NULL,
  access_key = NULL,
  secret_key = NULL,
  session_token = NULL,
  endpoint = NULL,
  path_style = NULL,
  anonymous = NULL,
  read_only = FALSE
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{bucket}}{Character string. The S3 bucket name.}
      \item{\code{prefix}}{Character string. The key prefix within the bucket that
acts as the root of this store (e.g. \code{"datasets/mycube/"}). Use \code{""}
for the bucket root. A trailing \code{/} is added if missing and the
prefix is non-empty.}
      \item{\code{region}}{Character string, AWS region, e.g. \code{"eu-west-1"}. If
\code{NULL}, resolved by \code{paws.storage} from the environment/config.}
      \item{\code{profile}}{Character string, a named profile from the shared AWS
credentials file. Ignored if \code{access_key} is supplied.}
      \item{\code{access_key, secret_key, session_token}}{Optional explicit
credentials, for cases where they must be threaded through from
elsewhere rather than resolved by \code{paws.storage} itself. Prefer
\code{profile} or the default provider chain over these.}
      \item{\code{endpoint}}{Character string. Optional custom endpoint URL, for
S3-compatible backends (MinIO, Ceph RGW, EMBASSY Cloud, etc.) rather
than AWS S3.}
      \item{\code{path_style}}{Logical. Force path-style addressing
(\code{endpoint/bucket/key}) instead of virtual-hosted-style
(\code{bucket.endpoint/key}). Non-AWS S3-compatible backends generally
only support path-style, so this defaults to \code{TRUE} whenever
\code{endpoint} is supplied, and \code{FALSE} (AWS default) otherwise. Set
explicitly to override.}
      \item{\code{anonymous}}{Logical. If \code{TRUE}, make unsigned requests, for
public buckets. If \code{FALSE}, use \code{paws.storage}'s normal credential
resolution (explicit \code{profile}/\code{access_key}, then its default
provider chain). If left \code{NULL} (the default), this is decided
automatically: \code{TRUE} when neither \code{profile} nor \code{access_key} was
given, \code{FALSE} otherwise. \code{paws.storage} does not fall back to
unsigned requests on its own — a call against a public bucket with
no credentials configured fails outright rather than trying
anonymously, which is what this default is for.}
      \item{\code{read_only}}{Logical. If \code{TRUE}, disable all write methods
(\code{set()}, \code{erase()}, etc. become no-ops), regardless of the
permissions of the underlying credentials. Default \code{FALSE}.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    An instance of this class.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-exists"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-exists}{}}}
\subsection{\code{zarr_s3store$exists()}}{
  Check if a key exists in the store.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$exists(key)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{key}}{Character string. The key that the store will be searched for.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{TRUE} if argument \code{key} is found, \code{FALSE} otherwise.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-clear"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-clear}{}}}
\subsection{\code{zarr_s3store$clear()}}{
  Remove all objects below this store's prefix. No-op if
the store was opened with \code{read_only = TRUE}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$clear()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{TRUE} on success, \code{FALSE} otherwise.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-erase"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-erase}{}}}
\subsection{\code{zarr_s3store$erase()}}{
  Remove a single key from the store. No-op if the store
was opened with \code{read_only = TRUE}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$erase(key)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{key}}{Character string. The key to remove.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{TRUE} on success, \code{FALSE} otherwise.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-erase_prefix"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-erase_prefix}{}}}
\subsection{\code{zarr_s3store$erase_prefix()}}{
  Remove all keys below a prefix. Batches deletes in groups
of up to 1000, the S3 \code{DeleteObjects} limit. No-op if the store was
opened with \code{read_only = TRUE}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$erase_prefix(prefix)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{prefix}}{Character string. The prefix whose keys to remove.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{TRUE} on success, \code{FALSE} otherwise.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-list_dir"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-list_dir}{}}}
\subsection{\code{zarr_s3store$list_dir()}}{
  Retrieve the immediate child nodes below a prefix
(single path component only), using S3's native \code{Delimiter} support.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$list_dir(prefix)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{prefix}}{Character string. The prefix whose nodes to list.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A character vector of node names immediately below \code{prefix}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-list_prefix"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-list_prefix}{}}}
\subsection{\code{zarr_s3store$list_prefix()}}{
  Retrieve all keys below a prefix (recursive).
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$list_prefix(prefix)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{prefix}}{Character string. The prefix whose nodes to list.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A character vector with all paths found below \code{prefix},
prefixed with \code{/} for consistency with \link{zarr_httpstore}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-list"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-list}{}}}
\subsection{\code{zarr_s3store$list()}}{
  Retrieve all keys in the store.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$list()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A character vector with all keys found in the store.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-getsize"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-getsize}{}}}
\subsection{\code{zarr_s3store$getsize()}}{
  Return the size, in bytes, of a value in the store, via
a \code{HeadObject} request (S3-native, no need to download the object).
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$getsize(key)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{key}}{Character string. The key whose length will be returned.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The size, in bytes, of the object.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-getsize_prefix"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-getsize_prefix}{}}}
\subsection{\code{zarr_s3store$getsize_prefix()}}{
  Return the total size, in bytes, of all objects found
under the group indicated by \code{prefix}, summed from a single
(paginated) \code{ListObjectsV2} scan rather than per-key \code{HeadObject}
calls.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$getsize_prefix(prefix)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{prefix}}{Character string. The prefix to groups to scan.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The total size, in bytes, as a single numeric value.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-is_empty"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-is_empty}{}}}
\subsection{\code{zarr_s3store$is_empty()}}{
  Is the group empty? Uses a single \code{MaxKeys = 1}
\code{ListObjectsV2} request rather than a full listing.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$is_empty(prefix)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{prefix}}{Character string. The prefix to the group to scan.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{TRUE} if the group indicated by argument \code{prefix} has no
sub-groups or arrays, \code{FALSE} otherwise.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-set"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-set}{}}}
\subsection{\code{zarr_s3store$set()}}{
  Store a \verb{(key, value)} pair. No-op if the store was
opened with \code{read_only = TRUE}.

Single-request upload only (S3's ~5 GB \code{PutObject} limit); this is
not expected to matter for typical Zarr chunk sizes. If you start
writing very large chunks (or use very large shards), this needs a
multipart-upload path instead — not implemented here.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$set(key, value)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{key}}{Character string. The key to write to.}
      \item{\code{value}}{Raw vector. The data to store.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    Self, invisibly.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-set_if_not_exists"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-set_if_not_exists}{}}}
\subsection{\code{zarr_s3store$set_if_not_exists()}}{
  Store a \verb{(key, value)} pair only if the key does not
already exist. No-op if the store was opened with \code{read_only = TRUE}.

Uses a conditional \code{PutObject} with \code{IfNoneMatch = "*"} for atomic
create-if-absent semantics — no separate existence check, no race
window. This is an AWS S3 feature added in August 2024; it requires
a \code{paws.storage} version whose S3 client exposes \code{IfNoneMatch} on
\code{put_object()}, and a backend that honors it. Most S3-compatible
services (older MinIO releases, some others) reject or ignore the
header — if you're targeting one of those, this will need a
fallback to a check-then-put instead, which reintroduces the race.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$set_if_not_exists(key, value)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{key}}{Character string. The key to write to.}
      \item{\code{value}}{Raw vector. The data to store.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    Self, invisibly.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-get"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-get}{}}}
\subsection{\code{zarr_s3store$get()}}{
  Retrieve the value associated with a given key.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$get(key, prototype = NULL, byte_range = NULL)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{key}}{Character string. The key for which to get data.}
      \item{\code{prototype}}{Ignored. The only buffer type that is supported maps
directly to an R raw vector.}
      \item{\code{byte_range}}{As in \link{zarr_httpstore}: \code{NULL} for the whole object,
a single positive integer for an open-ended range from that offset,
a single negative integer for the final N bytes, or a length-2
vector \code{c(start, end)} with an exclusive end.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A raw vector with the data pointed at by the key, or \code{NULL} if
the key does not exist.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-get_metadata"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-get_metadata}{}}}
\subsection{\code{zarr_s3store$get_metadata()}}{
  Retrieve the metadata document of the node at \code{prefix},
always presented in Zarr v.3 format.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$get_metadata(prefix)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{prefix}}{The prefix of the node whose metadata document to retrieve.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A list with the metadata, or \code{NULL} if the prefix does not
point to a Zarr group or array.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-set_metadata"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-set_metadata}{}}}
\subsection{\code{zarr_s3store$set_metadata()}}{
  Write the metadata document for the node at \code{prefix}.
No-op if the store was opened with \code{read_only = TRUE}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$set_metadata(prefix, metadata)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{prefix}}{The prefix of the node whose metadata document to write.}
      \item{\code{metadata}}{A list with the Zarr v.3-format metadata to write.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    Self, invisibly.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-is_group"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-is_group}{}}}
\subsection{\code{zarr_s3store$is_group()}}{
  Test if \code{path} is pointing to a Zarr group.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$is_group(path)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{path}}{The path to test.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{TRUE} if the \code{path} points to a Zarr group, \code{FALSE} otherwise.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-create_group"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-create_group}{}}}
\subsection{\code{zarr_s3store$create_group()}}{
  Create a new group in the store under \code{parent}. Errors
if the store was opened with \code{read_only = TRUE}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$create_group(parent, name)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{parent}}{Character string. Path of the parent node.}
      \item{\code{name}}{Character string. Name of the new group.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A list with the metadata of the newly-created group.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-zarr_s3store-create_array"></a>}}
\if{latex}{\out{\hypertarget{method-zarr_s3store-create_array}{}}}
\subsection{\code{zarr_s3store$create_array()}}{
  Create a new array in the store under \code{parent}. As with
\link{zarr_httpstore}, the abstract interface's \code{create_array(parent, name)} is extended here with an explicit \code{metadata} argument, since
array metadata (shape, dtype, chunking, codecs) cannot be inferred
and must be supplied by the caller. Errors if the store was opened
with \code{read_only = TRUE}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{zarr_s3store$create_array(parent, name, metadata)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{parent}}{Character string. Path of the parent node.}
      \item{\code{name}}{Character string. Name of the new array.}
      \item{\code{metadata}}{A list with the Zarr v.3-format array metadata.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A list with the metadata of the newly-created array, as written.
  }
}

}
