% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store_s3.R
\name{s3_list_dir}
\alias{s3_list_dir}
\title{List objects/prefixes in an S3 bucket}
\usage{
s3_list_dir(
  bucket,
  prefix = "",
  region = NULL,
  profile = NULL,
  access_key = NULL,
  secret_key = NULL,
  session_token = NULL,
  endpoint = NULL,
  path_style = NULL,
  anonymous = NULL
)
}
\arguments{
\item{bucket}{Character string. The S3 bucket name.}

\item{prefix}{Character string. The prefix to list under. Default \code{""}.}

\item{region, profile, access_key, secret_key, session_token, endpoint, anonymous, path_style}{As in \link{zarr_s3store}; same credential resolution and anonymous default.}
}
\value{
A character vector of keys/prefixes found immediately below \code{prefix}.
}
\description{
Lists the immediate contents under a prefix in an S3 bucket, without
requiring a valid Zarr store to be present at that prefix. Useful for
discovering the actual store path(s) within a bucket that hosts multiple
datasets, before calling \code{\link[=open_zarr]{open_zarr()}} or \code{zarr_s3store$new()}.
}
\examples{
# OME test data
(prefix <- s3_list_dir("ome-zarr-scivis", region = "us-east-1"))

}
